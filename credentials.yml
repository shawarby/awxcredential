---
  - name: Create Credentials
    hosts: localhost
    gather_facts: no
    collections:
      - awx.awx
    roles:
      - tower

    tasks:
      - name: Create an Inventory
        tower_inventory:
          name: "New Inventory"
          description: "test inv"
          organization: "New Org"
          state: present
